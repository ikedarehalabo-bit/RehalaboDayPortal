# Supabase（Dashboard → Settings → API から取得し、実値を .env.local に入れる）
# .env.local は git に載せない（.gitignore 済み）。service_role は秘匿。
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
